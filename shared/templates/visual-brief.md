# 视觉说明

## 视觉摘要

视觉目标：

报告放置位置：

HTML 展示位置：

## 视觉资产

| 视觉标题 | 类型 | 解释内容 | 为什么帮助理解推荐方案 | Markdown 放置位置 | HTML 展示位置 |
| --- | --- | --- | --- | --- | --- |

## 图片生成 Prompt / 制作方法

### 视觉 1

视觉标题：

类型：

图片生成 prompt / 制作方法：

Markdown 图片引用：

```markdown
![产品概念图](./assets/visual-brief.png)
```

fallback：

```mermaid
flowchart LR
  A["用户问题"] --> B["核心决策"]
  B --> C["MVP 路线"]
  C --> D["验证实验"]
```

## 生成策略

优先级：
有生图能力时生成真实图片资产；没有生图能力时，使用 Mermaid 制作决策地图、MVP 路线图或验证漏斗。

HTML 用途：
如果生成 `wheelwise-report.html`，本视觉说明应同时服务于 HTML 的决策地图、MVP 路线图、视觉说明或 Demo 截面。

## 决策解释

决策是什么：

为什么选择它：

为什么不选替代方案：

证据：

假设：

风险：

fallback：

信心等级：
